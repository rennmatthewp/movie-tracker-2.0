import * as api from './api';

//fetchAPi calls window fetch to movie database

//getFilms calls window fetch and clean films

//cleanFilms gets an array of uncleaned data, returns array with cleaned data

//getUserData calls window fetch to backend
//getUserData determines if user has already been created and if so, return out
//otherwise returns user object with favorites

//sendFavorite takes in user and film, and calls postFetch

//deleteFavorite calls delete fetch

//postFetch calls post window fetch

describe('api helper', () => {
  it('should fetch movie data', () => {
    
  })
})